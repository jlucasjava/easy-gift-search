{
  "name": "easy-gift-search",
  "version": "2.1.0",
  "description": "AI-powered gift finder with marketplace integration",
  "main": "index.html",
  "scripts": {
    "start": "npx serve public -s",
    "build": "echo 'Static site ready in public directory'",
    "dev": "python -m http.server 8000 --directory public",
    "serve": "npx serve public -s -p $PORT",
    "test": "echo 'No tests specified'",
    "minify:css": "npx --package=csso csso public/css/style.css --output public/css/style.min.css"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jlucasjava/easy-gift-search.git"
  },
  "keywords": [
    "gift",
    "search",
    "ai",
    "marketplace",
    "presents"
  ],
  "author": "Easy Gift Search Team",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "devDependencies": {
    "csso": "^5.0.5"
  }
}
